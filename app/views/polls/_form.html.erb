<%= form_for(@poll) do |f| %>
  <% if @poll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poll.errors.count, "error") %> prohibited this poll from being saved:</h2>

      <ul>
      <% @poll.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :poll_title %><br>
    <%= f.text_field :poll_title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>

	<%= f.select :category_id, @categories.collect {|p| [ p.category_title, p.id ] }, {:prompt => 'Bitte wählen'} %>

  </div>
  <div class="field">
    <%= f.label :key_user %><br>
    <%= f.text_field :key_user %>
  </div>
  <div class="field">
    <%= f.label :key_admin %><br>
    <%= f.text_field :key_admin %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
